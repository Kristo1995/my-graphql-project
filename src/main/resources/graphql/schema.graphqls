type Query {
    getUser(id: ID!): User
    getUsers: [User]
}

type Mutation {
    addUser(userRequest: UserRequest!): User
    deleteUser(id: ID!): String
}

type User {
    id: ID!
    name: String!
}

input UserRequest {
    name: String!
}